{
  "name": "cv-aitor",
  "version": "1.0.0",
  "private": true,
  "description": "CV de Aitor Izurrategui: datos en JSON + plantilla HTML/CSS, PDF y web estática (GitHub Pages)",
  "scripts": {
    "build": "node build.js",
    "dev": "node serve.js",
    "cv": "node build.js"
  },
  "dependencies": {
    "puppeteer-core": "^23.11.1"
  }
}
